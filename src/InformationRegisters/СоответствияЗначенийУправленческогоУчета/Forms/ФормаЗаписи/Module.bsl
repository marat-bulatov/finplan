
&НаСервере
Процедура РеквизитПриИзмененииНаСервере()
	
	Если ЗначениеЗаполнено(Запись.Реквизит) Тогда
		ТипЗначения = Запись.Реквизит.ТипЗначения;
	Иначе
		ТипЗначения = "";
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура РеквизитПриИзменении(Элемент)
	РеквизитПриИзмененииНаСервере();
КонецПроцедуры
