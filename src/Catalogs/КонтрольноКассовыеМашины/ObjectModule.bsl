
Процедура ПередЗаписью(Отказ)
	
	//Если НЕ ЗначениеЗаполнено(Наименование) Тогда
	//	Наименование = "" + Модель.Наименование + " " + ЗаводскойНомер;
	//КонецЕсли;
	
	Наименование = "" + Модель.Наименование + " " + ЗаводскойНомер;
	
КонецПроцедуры
