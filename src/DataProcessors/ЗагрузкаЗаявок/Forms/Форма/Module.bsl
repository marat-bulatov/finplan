&НаСервере
Процедура СтартНаСервере()
	
	Обработки.ЗагрузкаЗаявок.ЗагрузитьЗаявки(Период.ДатаНачала, Период.ДатаОкончания);
	
КонецПроцедуры

&НаКлиенте
Процедура Старт(Команда)
	СтартНаСервере();
	ОповеститьОбИзменении(Тип("ДокументСсылка.Заявка"));
КонецПроцедуры